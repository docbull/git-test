# IPFS_PATH=~/.ipfs ipfs repo gc
# IPFS_PATH=~/.ipfs2 ipfs repo gc
# IPFS_PATH=~/.ipfs3 ipfs repo gc
# IPFS_PATH=~/.ipfs4 ipfs repo gc
# IPFS_PATH=~/.ipfs5 ipfs repo gc
# IPFS_PATH=~/.ipfs6 ipfs repo gc
# IPFS_PATH=~/.ipfs7 ipfs repo gc
# IPFS_PATH=~/.ipfs8 ipfs repo gc
# IPFS_PATH=~/.ipfs9 ipfs repo gc

docker exec ipfs-host0 ipfs repo gc
docker exec ipfs-host1 ipfs repo gc
docker exec ipfs-host2 ipfs repo gc
docker exec ipfs-host3 ipfs repo gc
docker exec ipfs-host4 ipfs repo gc
docker exec ipfs-host5 ipfs repo gc
docker exec ipfs-host6 ipfs repo gc
docker exec ipfs-host7 ipfs repo gc
docker exec ipfs-host8 ipfs repo gc